# 迹目发布准备清单

本文件只记录发布候选版本需要完成的事项、当前状态和验收口径。产品方向写入 `AI_PLAN.md`，当前项目事实写入 `FACT_REPORT.md`，执行流水写入 `DEV_LOG.md`。

更新时间：2026-06-18

## 1. 发布目标

目标是让迹目从“可用的 debug / 内测版本”进入“可以给外部用户安装、测试和后续上架”的发布候选状态。

发布候选版本至少要做到：

- 有基本品牌识别，不再使用默认图标。
- 可以生成可安装的 release 包。
- 有明确版本号和签名策略。
- 有隐私政策和权限说明。
- 用户本地数据可以导出和导入。
- 深色模式、删除确认、基础导航和复盘主线不露明显问题。

## 2. 当前发布状态概览

| 阶段 | 状态 | 说明 |
|------|------|------|
| 产品主线验证 | 已完成 | T9/T10 已验证复盘减负和底部复盘入口 |
| 发行基础 | 进行中 | R3/R6/R7 已完成，R1/R2/R4/R5 待做 |
| 数据安全 | 部分完成 | R8 已完成，R9 基本路径和真机仪器测试通过，异常手测待收口；R10 待做 |
| 体验补齐 | 部分完成 | R11/R13 已完成，R12 核心页面完成但隐私入口待 R14 |
| 质量加固 | 待开始 | R15/R16/R17 待做 |

## 3. 已满足的发布前条件

- 核心功能闭环已经可用：待办、习惯、目标、复盘。
- 复盘历史列表已完成，支持指定日期编辑。
- 本地“今日成果草稿”已完成并通过真实手机验证。
- 底部第 5 栏已调整为“复盘”，已完成记录迁回待办页。
- 深色模式已修复，真实手机复测通过。
- 待办、习惯、目标删除入口均具备二次确认。
- 未使用的 `INTERNET` 权限已移除。
- 语音识别前已做联网预检。
- 待办到期提醒技术验证已完成，真实手机手测通过。

## 4. 必须完成项

### R1：App 图标设计与替换

状态：待做。

目标：

- 替换 Android Studio 默认机器人图标。
- 形成“迹目”的第一眼品牌识别。

验收口径：

- 启动器图标、最近任务图标、安装包图标都不再显示默认机器人。
- 图标在浅色、深色、圆形、方形、圆角方形图标背景下都可辨识。
- 生成或配置 adaptive icon 所需资源。

建议方向：

- 使用“路径 / 标记 / 目标 / J 字母”之一作为核心图形。
- 主色沿用当前蓝色体系，避免重新定义整套品牌色。

### R2：release 签名、混淆和版本策略

状态：待做。

目标：

- 可以构建可安装的 release 包。
- 明确 keystore 保管方式和版本号递增规则。

验收口径：

- release buildType 配置签名。
- keystore 不提交到仓库。
- 版本策略明确，例如 `versionCode` 每次发布递增，`versionName` 按 `主版本.次版本.修订号` 或轻量规则推进。
- 明确是否开启 R8 minify；如果开启，需要验证 Room、Compose、Navigation、反射相关路径不受影响。

### R4：Typography 中文优化

状态：待做。

目标：

- 让中文标题、正文、按钮和说明文字在现有 Material3 基础上更稳定、更易读。

验收口径：

- 定义项目自己的 Typography。
- 常用页面标题、卡片标题、正文、辅助说明和按钮文字视觉层级清楚。
- 不引入大规模 UI 重做。

### R5：启动画面

状态：待做。

目标：

- 补齐 Android 12+ SplashScreen 体验，减少冷启动白屏感。

验收口径：

- 冷启动时显示稳定的品牌启动画面。
- 启动画面图标与 R1 图标体系一致。
- 不影响主 Activity 正常启动。

## 5. 数据安全项

### R8：数据导出 JSON

状态：已完成。

已完成：

- 已实现 V1 JSON 备份 DTO 和编码器，覆盖待办、习惯、习惯记录、目标、目标步骤和复盘。
- 已实现设置页 SAF `CreateDocument("application/json")` 导出流程。
- 已实现 `jimu-backup-YYYYMMDD-HHmmss.json` 默认文件名、10 MiB 上限和明确失败提示。
- JVM codec、校验、受限流读写和 Repository 导出测试通过。
- 用户已在真机验证设置页导出基本路径成功。

目标：

- 用户可以把本地数据导出为 JSON 文件。

验收口径：

- 至少导出待办、习惯、习惯记录、目标、目标步骤、复盘。
- 导出格式包含版本号，方便后续兼容。
- 导出失败时有明确提示。
- 不依赖云服务。

### R9：数据导入 JSON

状态：部分完成。

已完成：

- 已实现 V1 格式识别、包名/主键/关系/标题/日期校验和全量替换策略。
- 已实现导入预览、明文风险提示和恢复前强制保存当前数据保险备份。
- 已实现 Room 事务恢复、恢复专用 `ABORT` 插入以及恢复成功后的待办提醒重建。
- Android JSON 往返、真实 Room 回滚和 SQLite 自增序列仪器测试已在小米 11 / Android 14 真机通过。
- 用户已在真机验证导入预览、恢复前保险备份和数据恢复基本路径成功。

待完成：

- 非法 JSON、超 10 MiB、保险备份写入失败和提醒到点行为仍需逐项确认。

目标：

- 用户可以从本地 JSON 文件恢复数据。

验收口径：

- 能识别 R8 导出的格式版本。
- 导入前提示风险。
- 导入失败不应破坏现有数据库。
- 对重复数据有明确策略，是覆盖、合并还是拒绝导入，不能含糊。

### R10：数据层安全债整改

状态：待做。

目标：

- 降低 Room 数据长期使用风险。

验收口径：

- 为 `habit_records(habitId, recordDate)` 建立唯一约束或等价去重策略。
- 为关键查询补必要索引。
- 如涉及 schema 变更，必须升级 Room version 并写 migration。
- 需要考虑历史重复打卡记录的清理策略。

## 6. 体验补齐项

### R11：复盘历史列表

状态：已完成。

说明：

- 已通过 T8 前置完成。
- 支持从今日复盘页进入历史列表，并点击指定日期编辑。

### R12：设置页

状态：部分完成。

已完成：

- 首页设置按钮已有明确落点。
- 设置页已显示版本信息和数据导出/导入入口。
- 设置页为二级页面，提供返回入口并隐藏底部导航。
- 用户已在真机验证设置页和数据备份基本路径成功。

待完成：

- 隐私政策入口仍属于 R14，当前设置页尚未提供。

目标：

- 承载版本信息、数据导出/导入入口、隐私政策入口和后续提醒说明入口。

验收口径：

- 首页设置按钮有明确落点。
- 设置页至少显示版本信息。
- 数据导出/导入可以从设置页进入。
- 隐私政策可以从设置页进入。

### R13：首页设置按钮接入

状态：已完成。

已完成：

- 首页右上角设置按钮已导航到 `Settings` 路由。
- `Settings` 路由不显示底部导航，返回使用 `popBackStack()`。
- 用户已在真机完成基本导航测试。

目标：

- 首页右上角设置按钮不再是空操作。

验收口径：

- 点击设置按钮进入设置页。
- 从设置页返回后，底部导航和首页状态正常。

### R14：隐私政策页面或文档

状态：待做。

目标：

- 清楚说明本地数据、录音权限、通知权限、精确闹钟权限和不做云同步的事实。

验收口径：

- 用户能在 App 内或可访问文档中看到隐私政策。
- 权限用途解释与实际 Manifest 保持一致。
- 不声称没有使用系统联网语音服务；应说明语音识别由系统服务提供，部分设备可能依赖联网。

## 7. 质量加固项

### R15：核心 ViewModel 单元测试

状态：待做。

建议覆盖：

- 待办新增、编辑、完成、回退、删除确认后的状态变化。
- 习惯打卡、取消打卡、删除确认。
- 目标步骤增删改和进度计算。
- 复盘保存、预填、历史日期编辑。

### R16：核心 Repository 单元测试

状态：待做。

建议覆盖：

- TaskRepository 的增删改查和完成状态。
- HabitRepository 的打卡、取消打卡和重复记录处理。
- GoalRepository 的目标与步骤关系。
- ReviewRepository 的按日期保存和更新。

### R17：公共 UI 组件抽取

状态：待做。

目标：

- 清理页面间重复的 `PageHeader` 等公共组件。

边界：

- 只抽取稳定重复组件。
- 不借机重做视觉风格。
- 不改变页面信息架构。

## 8. 暂不作为发布阻塞

以下事项不阻塞第一版发布候选：

- 搜索。
- 桌面 Widget。
- 习惯/目标提醒。
- 新用户引导页。
- 统计图表。
- mood 输入。
- 真实完成快照。
- 复盘删除。
- AI 总结。
- 云同步和账号系统。

## 9. 当前推荐顺序

推荐发布准备顺序：

1. R1 + R5：先定图标和启动画面，解决第一眼观感。
2. R2：配置 release 签名、版本策略和 release 构建。
3. R9：补齐异常路径和提醒到点验收。
4. R12 + R14：为设置页补隐私政策入口。
5. R10：处理数据层安全债，涉及 migration，需要单独谨慎推进。
6. R4、R15、R16、R17：继续打磨体验和质量。

如果只按风险排序，R8/R9 数据导出导入优先级最高；如果按发布外观排序，R1/R5/R2 应先做。
