# 迹目 发行规划分析

本文件是产品分析和发行路线规划，用于和 `AI_PLAN.md` 共同校准发布前的产品主线、发行风险和任务优先级。

创建时间：2026-06-15

---

## 第一部分：当前功能全景

### 已实现功能

| 模块 | 功能 | 完成度 |
|------|------|--------|
| 首页 | 今日待处理数量、已完成数量、当前目标进度、今日概览文案、今日复盘卡片、语音入口（待办/习惯/目标） | ✅ 完整 |
| 待办 | 新增（标题+日期+时间）、编辑、删除、标记完成、顺延（今天/明天/指定日期）、今日/全部切换、逾期/今日/计划中/未安排分组 | ✅ 完整 |
| 习惯 | 新增（名称+备注）、编辑、删除、今日打卡/取消打卡、连续天数、累计次数、今日/全部切换、今日进度统计 | ✅ 完整 |
| 目标 | 新增（标题）、编辑、删除、步骤管理（增删改+完成切换）、进度百分比、本周/本月/本年自动分组、整体推进概览 | ✅ 完整 |
| 已完成 | 完成记录列表（按日分组）、累计/今天/本周统计、回退待办状态、最近完成时间 | ✅ 完整 |
| 复盘 | 今日复盘编辑（做得好的事/遇到的问题/明日重点）、首页入口、保存后预填修改、历史列表、指定日期编辑 | ✅ 完整基础闭环 |
| 语音 | 语音新增待办（含时间解析）、语音新增习惯、语音新增目标、识别确认/编辑/重试 | ✅ 完整 |
| 提醒 | 待办到期提醒（精确闹钟+降级）、通知权限申请、启动/重启恢复、取消提醒 | ✅ 完整 |
| 底部导航 | 首页/待办/习惯/目标/已完成 5 个 tab | ✅ 完整 |

### 当前技术事实

- **包名**：`com.jimu.app`
- **版本**：versionCode=1, versionName="1.0"
- **minSdk**：26（Android 8.0）
- **targetSdk/compileSdk**：34（Android 14）
- **数据库**：Room version 5，6 张表
- **构建**：Debug 构建正常，release 未配置签名和混淆

---

## 第二部分：问题诊断

### A. UI / 视觉层面

| 编号 | 问题 | 严重度 | 说明 |
|------|------|--------|------|
| UI-1 | **App 图标是 Android Studio 默认机器人** | 🔴 高 | `ic_launcher_foreground.xml` 是经典 Android 机器人轮廓，不是自定义设计。发行必须换。 |
| UI-2 | **深色模式无效** | 🟡 中 | `Theme.kt` 第 53 行 `if (darkTheme) JimuLightColorScheme else JimuLightColorScheme`，两个分支都是浅色方案。深色方案 `JimuDarkColorScheme` 定义了但从未使用。 |
| UI-3 | **Typography 完全未定制** | 🟡 中 | `Type.kt` 只有 `val Typography = Typography()`，所有字号/行高/字重都是 Material3 默认值，中文显示可能偏大或间距不够舒适。 |
| UI-4 | **设置按钮是空壳** | 🟢 低 | 首页右上角齿轮图标 `onClick = { }`，没有任何功能。发行前要么实现要么移除。 |
| UI-5 | **没有启动画面（Splash Screen）** | 🟡 中 | 冷启动时会短暂白屏。Android 12+ 建议使用 SplashScreen API。 |
| UI-6 | **没有空状态插图** | 🟢 低 | 各页面的空状态只有纯文字，没有插图或更友好的视觉引导。 |
| UI-7 | **删除操作没有二次确认** | 🟡 中 | 待办的删除按钮直接删除，没有确认弹窗（习惯和目标有确认弹窗）。 |

### B. 功能缺失层面

| 编号 | 问题 | 严重度 | 说明 |
|------|------|--------|------|
| FN-1 | **没有数据备份/恢复** | 🔴 高 | 卸载即丢失所有数据。Room 底层 SQLite，至少需要导出/导入 JSON 或利用 Android 备份框架。 |
| FN-2 | **没有引导页 / 首次使用指引** | 🟡 中 | 新用户打开 App 不知道怎么用。至少需要一个简单的 3 步引导或首页提示。 |
| FN-3 | **复盘历史列表已补齐** | ✅ 已完成 | T8 / commit `66d8da8` 已新增复盘历史列表和指定日期编辑；未做复盘删除、真实快照、mood 或统计分析。 |
| FN-4 | **没有搜索功能** | 🟡 中 | 待办/习惯/目标多了之后找不到。 |
| FN-5 | **习惯没有频率设置** | 🟢 低 | 当前是"每天打卡"，不支持隔天、每周等。MVP 阶段可以接受。 |
| FN-6 | **目标没有截止日期** | 🟢 低 | 目标只有标题和步骤，没有时间维度。 |
| FN-7 | **没有 Widget（桌面小组件）** | 🟢 低 | 个人管理类 App 强需求，但可以后续做。 |
| FN-8 | **提醒只支持待办** | 🟢 低 | 习惯和目标暂不支持提醒，MVP 可接受。 |

### C. 质量 / 发行准备层面

| 编号 | 问题 | 严重度 | 说明 |
|------|------|--------|------|
| QA-1 | **release 构建未配置** | 🔴 高 | `isMinifyEnabled = false`，无签名配置，无 ProGuard 规则。无法发布到应用商店。 |
| QA-2 | **单元测试覆盖不足** | 🟡 中 | 当前只有 7 个测试文件，覆盖复盘 ViewModel、提醒逻辑和 TaskRepository。待办/习惯/目标的 ViewModel 和 Repository 没有测试。 |
| QA-3 | **没有 UI 测试** | 🟡 中 | 零 Compose UI 测试。 |
| QA-4 | **没有隐私政策** | 🔴 高 | 如果要上架 Google Play 或国内商店，必须有隐私政策页面。 |
| QA-5 | **没有版本管理策略** | 🟡 中 | versionCode=1, versionName="1.0"，需要定义版本号递增规则。 |
| QA-6 | **数据层安全债未清** | 🟡 中 | habit_records 无唯一约束、无外键、无索引、insertHabitRecord 去重问题、历史脏数据。 |

### D. 架构 / 代码质量层面

| 编号 | 问题 | 严重度 | 说明 |
|------|------|--------|------|
| AR-1 | **ViewModel 直接依赖具体实现** | 🟢 低 | 通过 `JimuApp` 暴露 Repository 实例，没有依赖注入。当前规模可接受。 |
| AR-2 | **每个页面自定义 PageHeader** | 🟢 低 | 多个 Screen 各自定义了 `PageHeader` composable，有代码重复。 |
| AR-3 | **日期处理混用 java.util.Calendar 和 java.time** | 🟢 低 | `CompletedScreen` 用 Calendar，其他用 LocalDate。风格不统一但功能正确。 |
| AR-4 | **INTERNET 权限未使用** | 🟢 低 | Manifest 声明了 INTERNET 权限但当前无网络功能。如果确定不做云同步，应移除。 |

### E. 发布前产品主线风险

| 编号 | 风险 | 严重度 | 说明 |
|------|------|--------|------|
| PR-1 | **复盘减负不足** | 🔴 高 | 复盘已经能保存和回看，但主要依赖用户手填。App 已有待办、习惯、目标数据，却尚未自动整理成复盘素材。 |
| PR-2 | **“已完成”底部 tab 权重过高** | 🟡 中 | 当前“已完成”更接近已完成待办列表，没有承载习惯打卡、目标推进和复盘沉淀。直接保留为底部一级入口会削弱“成果/回顾”的主线。 |
| PR-3 | **首页反馈仍偏仪表盘** | 🟡 中 | 首页展示今日待处理、已完成数量和目标焦点，但还没有形成“今天推进得怎么样、晚上如何复盘”的自动反馈。 |

产品主线判断：第一版最大的留存风险不是功能缺口，而是用户录入后得到的自动反馈不足。发布准备前应先做 T9 本地复盘草稿 MVP，验证“自动整理 70%，用户补充 30%”是否能成立。

---

## 第三部分：发行需要达到的最低标准

要让迹目从"能用的 debug 版"变成"可以给人用的发行版"，至少需要完成以下事项：

### 发布准备前置验证

0. **T9 本地复盘草稿 MVP** — 先在现有 `ReviewScreen` 顶部展示“今日成果草稿”，用现有数据聚合今日待办、习惯打卡、目标步骤推进和未完成/逾期提醒；不改数据库、不新增 `completedAt`、不替换底部导航。

### 必做（不做到不能发行）

1. **App 图标** — 设计一个符合产品气质的自定义图标，替换默认机器人
2. **release 签名配置** — 生成 keystore，配置 release buildType 签名
3. **混淆配置** — `isMinifyEnabled = true`，配置 ProGuard/R8 规则（Room、Compose 需要保留规则）
4. **版本号** — 从 1.0 开始，定义 versionCode/versionName 策略
5. **隐私政策** — 至少一个本地 HTML 页面或简单网页
6. **深色模式修复** — 至少让已定义的深色方案生效
7. **数据备份** — 至少支持 JSON 导出/导入，防止卸载丢数据

### 应做（做到会大幅提升体验）

8. **启动画面** — Android 12+ SplashScreen API
9. **Typography 定制** — 针对中文优化字号和行高
10. **复盘历史列表** — 已通过 T8 / commit `66d8da8` 完成
11. **待办删除二次确认** — 与习惯/目标保持一致
12. **数据层安全债** — habit_records 唯一约束、索引
13. **单元测试补充** — 至少覆盖核心 ViewModel 和 Repository

### 可选（后续版本再做）

14. 搜索功能
15. 桌面小组件
16. 习惯/目标提醒
17. 引导页
18. 数据统计图表

---

## 第四部分：发行路线建议

### 阶段零：产品主线验证（T9）

> 目标：在进入发行基础工作前，先验证“复盘减负”和“今日成果反馈”是否能成为迹目的核心留存价值。

| 任务 | 内容 | 预估工作量 |
|------|------|-----------|
| T9 | 本地复盘草稿 MVP | 中等；新增业务聚合层和复盘页草稿展示，不改 Room schema |

T9 边界：

- 先不动数据库，不加 `completedAt`，避免牵涉 Room migration。
- 先不把底部“已完成”替换成“复盘”，避免价值未验证前大改导航。
- 今日完成待办先使用低风险近似口径，不承诺精确完成时间。
- 不引入云服务、大模型 API、复杂统计图或 AI 总结。

### 阶段一：发行基础（R1-R7）

> 目标：让 App 可以签名打包，具备发行的基本条件。

| 任务 | 内容 | 预估工作量 |
|------|------|-----------|
| R1 | App 图标设计与替换 | 需要你提供设计方案或让 AI 生成 |
| R2 | release 签名 + 混淆配置 | 中等 |
| R3 | 深色模式修复 | 小 |
| R4 | Typography 中文优化 | 小 |
| R5 | 启动画面 | 小 |
| R6 | 待办删除二次确认 | 小 |
| R7 | 移除未使用的 INTERNET 权限（或保留备用） | 极小 |

### 阶段二：数据安全（R8-R10）

> 目标：用户数据不会因操作失误或卸载丢失。

| 任务 | 内容 | 预估工作量 |
|------|------|-----------|
| R8 | 数据导出（JSON） | 中等 |
| R9 | 数据导入（JSON） | 中等 |
| R10 | 数据层安全债（唯一约束、索引） | 中等，需要 migration |

### 阶段三：体验补齐（R11-R14）

> 目标：让现有功能更完整、更好用。

| 任务 | 内容 | 预估工作量 |
|------|------|-----------|
| R11 | 复盘历史列表 | 已完成（T8 前置完成，commit `66d8da8`） |
| R12 | 设置页（占位+版本信息+数据导出入口） | 中等 |
| R13 | 首页设置按钮接入设置页 | 小 |
| R14 | 隐私政策页面 | 小 |

### 阶段四：质量加固（R15-R17）

> 目标：提升代码质量和可维护性。

| 任务 | 内容 | 预估工作量 |
|------|------|-----------|
| R15 | 核心 ViewModel 单元测试 | 中等 |
| R16 | 核心 Repository 单元测试 | 中等 |
| R17 | PageHeader 等公共组件抽取 | 小 |

---

## 第五部分：关于 UI 重做的判断

### 当前 UI 的实际水平

读完所有 Screen 代码后，我的判断是：**当前 UI 不需要推倒重做，但需要针对性打磨。**

理由：

1. **基础框架是好的** — 使用 Material3 + Compose，圆角卡片、动画过渡、色彩体系统一，比很多个人项目要好。
2. **蓝色主题有辨识度** — `OceanBlue` 作为主色调、`PanelBlue` 作为卡片底色，整体风格偏清爽，适合"个人推进工具"的产品定位。
3. **交互逻辑清晰** — 待办分组（逾期/今日/计划中/未安排）、习惯今日/全部切换、目标按周/月/年分组，这些设计是合理的。
4. **动画使用得当** — 首页卡片有入场动画，切换视图有过渡动画，不花哨但有品质感。

### 需要打磨的点

1. **App 图标必须换** — 这是用户看到的第一个东西，默认机器人图标直接暴露"这是开发者随手建的项目"。
2. **深色模式必须修** — 2026 年了，没有深色模式的 App 会显得不专业。
3. **Typography 需要调** — 默认 Material3 字号对中文不一定最优，需要微调标题和正文的字号/行高。
4. **空状态可以加插图** — 纯文字空状态显得单薄，加一个简单的 vector 插图会好很多。
5. **删除确认统一** — 待办直接删、习惯/目标有确认弹窗，体验不一致。

### 不建议做的事

- 不建议换配色方案 — 当前蓝色系已经建立了一致性。
- 不建议换组件库 — Material3 已经够用。
- 不建议做复杂的主题系统 — 单一主题 + 深色模式足够。
- 不建议重写所有页面 — 当前页面结构合理，只需微调。

---

## 第六部分：App 图标建议

当前图标是 Android Studio 默认的机器人图标（`ic_launcher_foreground.xml` 中是经典 Android 机器人路径数据）。

### 设计方向建议

"迹目"的产品含义是"留下痕迹、把握目标"。图标可以考虑：

1. **足迹 + 方向** — 一个简化的脚印或步伐图标，暗示"推进"
2. **路径/轨道** — 一条向前延伸的路径，暗示"前进"
3. **书签/标记** — 与"迹"呼应，暗示"记录"
4. **极简字母 J** — 取"迹目"首字母，做成现代感的 monogram

### 技术要求

- 需要提供 5 种密度的 PNG（mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi）+ adaptive icon 的 foreground/background XML
- 建议使用 Android Studio 的 Image Asset Studio 生成
- 前景色建议使用品牌蓝色 `#1E4D8F` 或 `#2563EB`
- 背景色建议使用白色或浅蓝 `#F3F7FC`

---

## 第七部分：下一步行动建议

如果你认可这份分析，建议按以下顺序推进：

1. **先做 T9 本地复盘草稿 MVP** — 在不改数据库、不动底部导航的前提下，验证复盘减负是否真的有价值。
2. **评估底部导航重排** — T9 成立后，再考虑 T10：把“已完成”降级为复盘页子模块，并将底部第 5 栏改为“复盘/回顾”。
3. **再处理 R1-R7 发行基础** — 图标、签名配置、深色模式、Typography、启动画面、删除确认、权限清理。
4. **补齐 R8-R10 数据安全** — 数据导出/导入和数据层安全债。
5. **补设置页、隐私政策和质量加固** — R12-R17；R11 复盘历史列表已通过 T8 完成。

---

*本文件不修改已提交的文档，仅作为规划参考。*
