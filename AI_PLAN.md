# AI_PLAN.md

本文件用于记录“迹目”的产品决策和开发路线。T1-T10 已完成，MVP 功能闭环、复盘回顾主线和底部导航重排均已收口；下一步进入 R1-R7 发布基础补齐。更详细的发行分析参考 `RELEASE_PLAN.md`。

## 产品定位

迹目是一个本地优先的个人推进工具，核心目标不是做复杂团队协作，而是帮助一个人把日程、习惯、目标和复盘串起来。

它应该像一个随身的“推进面板”：今天做什么、习惯有没有坚持、目标推进到哪一步、完成后能不能回看。

## 已确定的产品决策

- 先做单人本地 App，不做账号、云同步、团队协作。
- 继续使用当前 Android 原生技术栈：Kotlin、Compose、Room、Navigation Compose。
- 首页保留为总览入口，重点展示今日待处理、已完成数量、当前目标进度。
- 待办保持“逾期仍保留”的逻辑，适合真实生活里任务会延期的情况。
- 习惯先按“每天一次打卡”处理，不急着支持复杂频率。
- 目标采用“目标 + 步骤 + 进度”的模型，比单纯写一个目标更可执行。
- 语音作为快速录入入口，先服务待办、习惯、目标，不急着做复杂 AI 助手。
- 复盘是下一阶段关键能力，但先做最小可用版本。

## 当前已经具备的基础

- 底部 5 个页面：首页、待办、习惯、目标、已完成。
- Room 已有 5 张表：tasks、habits、habit_records、goals、goal_steps。
- 待办支持新增、编辑、删除、完成、延期、日期和时间。
- 习惯支持新增、编辑、删除、今日打卡、连续天数和累计次数。
- 目标支持新增、编辑、删除、步骤管理和进度统计。
- 已完成页支持查看完成记录和回退待办状态。
- 首页有语音新增入口。
- Room 已从 version 4 迁移到 version 5，已建立 schema 基线并写入第一条正式 migration。
- 复盘数据层已具备：`daily_reviews` 表、`ReviewEntity`、`ReviewDao`、`ReviewRepository`。
- 复盘闭环已具备：首页“今日复盘”卡片、今日复盘编辑页、保存后首页显示摘要、再次进入预填修改、历史复盘列表、指定日期编辑，以及今日复盘页顶部的本地“今日成果草稿”。

## 近期路线

当前阶段判断：T1-T10 已经收口，MVP 功能闭环、复盘“回看”能力、本地“今日成果草稿”和底部“复盘”一级入口均已具备。T10 已采用方案 A 落地并完成收尾：底部第 5 栏从“已完成”改为“复盘”，已完成记录迁回待办页“已完成”分支，首页完成卡片改为保守的“当前已完成”口径，首页“今日概览”复述卡已取消，首页复盘卡与底栏复盘共用一级 tab 导航，复盘 tab 模式不再显示返回按钮且保存后留在复盘页，历史日期复盘编辑页仅保留返回入口且保存后也留在当前页。T10 不引入基于 `updatedAt` 的今日/本周完成统计，主体流程已通过用户真实手机手测；历史日期编辑页交互微调已完成本地验证，建议快速复测该路径。下一步进入 R1-R7 发布基础补齐。

### 1. 先整理基础可靠性

目标：让项目更适合继续开发，不先堆新功能。

- T1：已完成 - 修复首页重复 `VoiceInputSheet`，`assembleDebug` 已通过。
- T2：已完成 - 目标语音用独立状态，不再复用 `HabitReview`。
- T2.5：暂时跳过 - 不在 T3 前升级 Android Gradle Plugin。
  - 当前 `assembleDebug` 已通过。
  - AGP 8.5.1 + Gradle 9.3.1 虽然存在版本矩阵错配，但当前可用。
  - AGP 升级会牵涉 Kotlin / KSP / Compose compiler / plugin 迁移。
  - 不应在 T3 / T4 之前引入构建工具链风险。
- T3：已完成 - 习惯支持取消今日打卡，commit `ad741b6`，`assembleDebug` 已通过。
- T4：已完成 - Room 数据安全最小整改，commit `0b85988`，已建立 Room version 4 schema 基线，`exportSchema = true`，已移除破坏性迁移 fallback，`assembleDebug` 已通过。
- T5：已完成 - 复盘数据层与 Room 4 到 5 迁移，commit `3b26779`，新增 `daily_reviews` 表、`MIGRATION_4_5`、`ReviewRepository`，`testDebugUnitTest` 和 `assembleDebug` 已通过。

T5 后当前数据库安全状态：

- 已有 Room version 4 和 version 5 schema。
- 已有第一条正式 migration：`MIGRATION_4_5`，只创建 `daily_reviews` 表，不修改旧表。
- 未来数据库结构变更必须写 migration，不再依赖破坏性删库。
- 暂未处理 `habit_records(habitId, recordDate)` 唯一约束、子表外键、索引、删除习惯/目标的显式事务、`insertHabitRecord` 去重问题和历史脏数据清理。
- v4 旧库覆盖安装到 v5 的设备/模拟器回归尚未补测；当前记录的限制是命令行 `adb` 不在 PATH。

### 2. 做复盘最小版本

目标：让“迹目”真正覆盖计划到回看的闭环。

当前状态：

- T5：已完成复盘数据层，commit `3b26779`。
- T6：已完成复盘最小闭环，commit `6c6237e`，新增首页入口、今日复盘编辑页、今日复盘 ViewModel 与表单状态测试，`testDebugUnitTest` 和 `assembleDebug` 已通过。
- T6 已在真实手机完成手测。

最小版本只需要：

- 新增复盘数据表。已在 T5 完成数据层。
- 支持写一条今日复盘。已在 T6 完成。
- 字段先保持简单：日期、做得好的事、遇到的问题、明日重点。已在 T6 完成。
- 在 UI 中提供入口。已在 T6 通过首页“今日复盘”卡片进入，不加入底部 tab。

- T8：已完成 - 发布准备前的小补齐任务 / R11 前置完成，commit `66d8da8`，新增复盘历史列表、指定日期复盘编辑、首页复盘三段摘要预览和从复盘页点击底部首页回到首页顶部的导航策略；`testDebugUnitTest` 和 `assembleDebug` 已通过，已真机手测通过并推送到 `origin/main`。

暂时不做 mood 输入、真实快照、复盘删除、复杂模板、AI 总结、图表分析。

### 3. 产品主线重排：T9 复盘草稿 MVP

目标：先验证“迹目”能否从记录工具升级为复盘助手，再大改底部导航或进入发布准备。

- T9：已完成 - 本地复盘草稿 MVP。
  - 新增 `DailyDigestRepository` 和 `DailyDigestBuilder` 作为业务聚合层。
  - 聚合当前已完成待办概览、今日习惯打卡、当前目标步骤推进、今日/逾期未完成提醒。
  - 在现有 `ReviewScreen` 顶部展示“今日成果草稿”，下面保留三段手填复盘。
  - 待办口径使用“当前已完成”而不是“今天完成”，避免误导为精确完成时间统计。
  - 未改 `ReviewEntity`，未升级 Room version，未写 migration，未新增 `completedAt`。
  - 未引入云服务、大模型 API、复杂统计图、复盘删除或复杂模板。
  - T9 当时未把底部“已完成”替换成“复盘”，避免在价值验证前做导航大改；T10 已在价值验证后完成该导航重排。
  - `testDebugUnitTest` 和 `assembleDebug` 已通过；真实手机验证通过。
  - T9 收口后发现并修复一个底部首页 tab 状态恢复问题：从待办/目标等 tab 点击“首页”时不再错误恢复到刚才的 tab。

T9 验证后的结论：

- T9 复盘草稿已通过真实手机验证，复盘减负方向成立。
- T10：已完成 - 采用方案 A + 保守统计口径并完成收尾。底部第 5 栏改为“复盘”；待办页新增“已完成”分支承接完成记录和回退；首页“完成节奏”只展示当前已完成、当前待处理、目标推进和今日复盘状态，不做今日/本周完成统计；首页“今日概览”卡已取消；首页复盘卡和底栏复盘入口已统一为 tab 导航；`ReviewScreen` 已区分一级 tab 和历史日期编辑页模式，保存与返回动作已拆开；未改 Room，未引入 AI/图表，未删除 `CompletedScreen.kt`；主体流程真实手机手测通过，历史日期编辑页微调已本地验证。

### 4. 做提醒能力的技术验证

目标：验证待办到期提醒是否可行。

- 先选一个最简单的提醒路径。
- 明确 Android 版本权限要求。
- 先支持待办提醒，不扩展到习惯和目标。
- 不急着做重复提醒、智能提醒、复杂通知分类。
- T7：已完成 - 待办到期提醒技术验证，已真机手测通过。
  - 已接入 Android 13+ `POST_NOTIFICATIONS` 运行时权限、高优先级通知 channel、`BroadcastReceiver` 和待办新增/编辑/完成/删除时的提醒安排/取消。
  - 真机准点性验证发现非精确 `AlarmManager.setAndAllowWhileIdle` 对 19:12 待办延迟到 19:13:29 投递，无法承诺严格准点；因此改为优先 `setExactAndAllowWhileIdle`，无 `SCHEDULE_EXACT_ALARM` 特殊访问时降级到 `setAndAllowWhileIdle`，并在用户主动安排提醒时引导到系统授权页。
  - 已新增启动时未来提醒重排、`BOOT_COMPLETED` / `MY_PACKAGE_REPLACED` / 精确闹钟授权变化后的未来提醒重排。
  - 已统一 reminder id 口径（`TaskReminderIds` 直接用 task id，超 Int 范围返回 null），避免 schedule/cancel 使用分散的 `hashCode()` 规则。
  - 声音修复：旧通知 channel 一旦创建，其重要性/声音/震动会被系统锁定、代码改不动；为强制重建带声音的高优先级 channel，channel id 升级到 `task_due_reminders_v3`，并显式设置默认通知音、`AudioAttributes` 和震动模式。
  - 未改 Room schema，未写 migration，未升级工具链，未扩展到习惯/目标。
  - `testDebugUnitTest` 和 `assembleDebug` 已通过。
  - 真机手测结论：打开系统“闹钟和提醒”授权 + 关闭电池智能优化 + 通知音量非 0 后，后台/锁屏到点精准触发，声音正常，系统静音时同步静音（走标准通知音量通道，行为正确）。
  - 已知限制：非精确降级路径不承诺严格准点；部分国产 ROM 的电池策略可能压制后台触发，需用户手动关闭电池优化或加白名单，当前未在代码中引导该项。

### 5. 后续技术债：构建工具链升级

目标：在 MVP 稳定后，单独处理 AGP / Kotlin / KSP / Compose 构建工具链升级。

- 当前不在 T3 / T4 之前升级 Android Gradle Plugin。
- 后续升级应作为独立技术债任务处理，避免把构建工具链风险混入功能开发。
- 升级时需要统一评估 AGP、Kotlin、KSP、Compose compiler / plugin 的版本兼容关系。

## 暂不做的事情

- 不做账号系统。
- 不做云同步。
- 不做社交、打卡排行、团队协作。
- 不做复杂数据分析大屏。
- 不引入大模型 API 作为核心依赖。
- 不大改 UI 风格。
- 不为了“架构漂亮”做大规模重构。

## 发布前最低标准

目标：让“迹目”从可用的 debug / 内测版本，进入可以给外部用户安装、测试和后续上架的发行准备状态。

发布准备前置验证：

- T9 本地复盘草稿 MVP：先用现有数据生成“今日成果草稿”，验证复盘减负是否成立；T9 不改数据库、不改底部导航、不引入 AI。

必须完成：

- App 图标：替换 Android Studio 默认机器人图标，建立第一眼品牌识别。
- release 签名与版本策略：生成并安全保管 keystore，配置 release 签名，明确 `versionCode` / `versionName` 递增规则。
- 隐私政策与权限说明：说明本地数据、录音权限、通知权限、精确闹钟权限和当前不做云同步的事实。
- 深色模式修复：当前已定义 `JimuDarkColorScheme`，但主题分支仍返回浅色方案，发布前需要修正。
- 数据导出/导入：至少支持本地 JSON 导出/导入，避免用户因卸载、换机或误操作永久丢数据。

强烈建议完成：

- Typography 中文优化：在 Material3 默认字重/字号基础上微调中文阅读体验。
- 启动画面：补齐 Android 12+ SplashScreen 体验，减少冷启动白屏感。
- 待办和习惯删除二次确认：当前目标已有删除确认，待办和习惯仍是直接删除，需要统一高风险操作体验。
- 设置页：承载版本信息、数据导出/导入入口、隐私政策入口和后续提醒/电池优化说明。
- 复盘历史列表：已通过 T8 / commit `66d8da8` 完成，支持从今日复盘页进入历史列表并点击指定日期编辑。

可后续版本再做：

- 搜索功能。
- 桌面 Widget。
- 习惯/目标提醒。
- 引导页。
- 统计图表。
- 复杂主题系统或大规模 UI 重做。

## 发布准备路线

发布准备当前主线调整为：R1-R7 发行基础 -> R8-R10 数据安全 -> R12-R17 体验与质量补齐。T10 底部导航重排已完成。

### 阶段零：产品主线验证（T9）

目标：验证“每天晚上自动帮我整理 70%，我只补 30%”是否能成为迹目的核心留存价值。

- T9：已完成 - 本地复盘草稿 MVP；真实手机验证通过。
- 边界：只做现有数据聚合和复盘页顶部草稿展示，不改 Room schema，不新增底部 tab，不替换“已完成”入口。
- T10：已完成 - 底部“已完成”升级为“复盘”入口；已完成记录迁回待办页；首页完成卡片改为保守口径，不做基于 `updatedAt` 的时间统计；首页概览卡已取消；复盘入口统一为一级 tab；历史日期复盘编辑页隐藏“历史”入口，保存按钮为“保存”且保存后留在当前页；旧 `CompletedScreen` 内部的今日/本周完成统计病灶已清理但文件保留；主体流程真实手机手测通过，历史日期编辑页微调已本地验证。

### 阶段一：发行基础（R1-R7）

目标：让 App 可以形成一个有品牌识别、能签名安装、基础体验不露怯的发布候选版本。

- R1：App 图标设计与替换。
- R2：release 签名配置与版本号策略。
- R3：深色模式修复。
- R4：Typography 中文优化。
- R5：启动画面。
- R6：待办和习惯删除二次确认。
- R7：清理未使用的 `INTERNET` 权限，或明确保留理由。

### 阶段二：数据安全（R8-R10）

目标：保护用户本地数据，不让“卸载即丢失”成为发布版本的核心风险。

- R8：数据导出（JSON）。
- R9：数据导入（JSON）。
- R10：数据层安全债整改：`habit_records(habitId, recordDate)` 唯一约束、必要索引、去重策略和 migration。

### 阶段三：体验补齐（R11-R14）

目标：让现有能力更完整，而不是继续扩张新领域。

- R11：已完成 - 复盘历史列表（通过 T8 前置完成，commit `66d8da8`）。
- R12：设置页（版本信息、数据入口、隐私政策入口）。
- R13：首页设置按钮接入设置页。
- R14：隐私政策页面或可访问的隐私政策文档。

### 阶段四：质量加固（R15-R17）

目标：降低后续发布迭代风险。

- R15：核心 ViewModel 单元测试。
- R16：核心 Repository 单元测试。
- R17：公共 UI 组件抽取，例如各页面重复的 `PageHeader`。

## 发布路线判断

当前不建议推倒重做 UI。底部“已完成”替换为“复盘”的 T10 信息架构重排已完成，现有 Material3 + Compose 结构、蓝色主题、页面分组和动画基础可以继续沿用；发布前应重点做图标、深色模式、Typography、删除确认、设置页和数据安全。

不建议在发布准备阶段引入账号系统、云同步、团队协作、大模型 API、复杂统计大屏、复杂主题系统或大规模架构重构。

## 推荐下一步

优先顺序：

1. R1-R7：补齐 App 图标、release 签名、深色模式、Typography、启动画面、删除确认和 `INTERNET` 权限清理。
2. R8-R10：做数据导出/导入和数据层安全债整改。
3. R12-R14：补设置页、隐私政策；R11 复盘历史列表已在 T8 完成。
4. R15-R17：补核心测试和公共 UI 组件整理。

这样推进的好处是：T1-T8 已经证明核心产品闭环和回看能力可用；T9 先验证“每天打开是否更有用”，再处理“能否放心交给别人安装和长期使用”的发布问题。
