# 迹目产品路线图

本文件只记录“迹目”的产品方向、阶段判断和后续路线。执行流水写入 `DEV_LOG.md`，当前事实快照写入 `FACT_REPORT.md`，发布准备清单写入 `RELEASE_PLAN.md`。

更新时间：2026-06-18

## 1. 产品定位

迹目是一个本地优先的 Android 个人推进工具，用来把待办、习惯、目标和复盘串起来。

它的核心不是团队协作，也不是复杂数据分析，而是帮助一个人回答四个日常问题：

- 今天要推进什么。
- 习惯有没有坚持。
- 目标推进到哪一步。
- 完成之后能不能回看和复盘。

第一版应优先把“记录 -> 推进 -> 回看 -> 复盘”的闭环做稳，而不是继续扩张新领域。

## 2. 长期产品边界

当前阶段坚持以下边界：

- 先做单人本地 App，不做账号、云同步和团队协作。
- 继续使用 Kotlin、Jetpack Compose、Room、Navigation Compose。
- 语音只作为快速录入口，先服务待办、习惯、目标。
- 复盘优先做本地减负：先用已有数据生成“今日成果草稿”，用户再补充文字。
- 不引入大模型 API、云服务、复杂统计大屏或大规模 UI 重做。
- 不为了架构漂亮引入 Hilt、Dagger、Koin 等新框架。

## 3. 当前阶段判断

T1-T10 已经收口，MVP 主线成立：

- 待办、习惯、目标的基础增删改查和状态切换已经可用。
- Room 已建立 version 4 / version 5 schema 基线，并完成 `MIGRATION_4_5`。
- 今日复盘、历史复盘、指定日期编辑和本地“今日成果草稿”已经完成。
- 底部导航已从“已完成”调整为“复盘”，已完成记录迁回待办页的“已完成”分支。
- 待办提醒技术验证已完成，真实手机手测通过。
- 深色模式、删除二次确认、无用 `INTERNET` 权限清理和语音联网预检已完成。

当前主线应从“继续加功能”切换到“发布准备和数据安全”。下一阶段最重要的不是新功能数量，而是让 App 可以被放心安装、备份、升级和长期使用。

本地备份恢复 MVP 已进入验收收口阶段：

- R8 已完成，用户已在真机验证导出基本路径成功。
- R9 的导入预览、恢复前保险备份和数据恢复基本路径已在真机成功。
- R13 设置入口与返回基本导航已在真机成功；R12 仍待 R14 隐私政策入口。
- 真实 Room 仪器测试、异常文件路径和提醒到点行为仍需收口。
- R10 的唯一约束、索引、去重和 migration 仍保持为独立数据安全任务。

## 4. 已完成里程碑

### MVP 和复盘主线

- T1：清理首页重复语音弹窗。
- T2：目标语音新增改为独立状态。
- T3：习惯支持取消今日打卡。
- T4：Room schema 基线与破坏性迁移 fallback 移除。
- T5：新增复盘数据层，Room 从 version 4 迁移到 version 5。
- T6：今日复盘最小闭环。
- T7：待办到期提醒技术验证。
- T8 / R11：复盘历史列表和指定日期编辑。
- T9：本地“今日成果草稿”MVP。
- T10：底部“复盘”一级入口与已完成记录迁移。

### 发布基础已完成项

- R3：深色模式修复，主题默认跟随系统深色模式。
- R6：待办和习惯删除二次确认。
- R7：移除未使用的 `INTERNET` 权限，并为系统语音识别补充联网预检。

## 5. 当前优先路线

### 阶段一：发布基础补齐

目标：让 App 具备基本发行形态，不再暴露明显的“开发中样子”。

优先级：

1. R1：替换默认 App 图标，建立第一眼品牌识别。
2. R2：配置 release 签名、版本号策略和基础混淆策略。
3. R4：Typography 中文阅读体验优化。
4. R5：Android 12+ 启动画面。

### 阶段二：数据安全

目标：避免用户因为卸载、换机或误操作永久丢失本地数据。

优先级：

1. 收口 R9：完成真实 Room 回滚、自增序列、异常文件和提醒重建验证。
2. R10：数据层安全债整改，包括 `habit_records(habitId, recordDate)` 唯一约束、必要索引、去重策略和 migration。

### 阶段三：发布体验补齐

目标：让现有功能更完整，而不是继续扩张产品边界。

优先级：

1. R14：隐私政策页面或可访问文档，并从设置页提供入口，收口 R12。

### 阶段四：质量加固

目标：降低后续迭代风险。

优先级：

1. R15：核心 ViewModel 单元测试。
2. R16：核心 Repository 单元测试。
3. R17：公共 UI 组件抽取，例如重复的 `PageHeader`。

## 6. 暂不进入的方向

以下内容可以作为后续版本方向，但不应进入当前发布准备主线：

- 账号系统和云同步。
- 社交、排行、团队协作。
- 搜索功能。
- 桌面 Widget。
- 习惯/目标提醒。
- 引导页。
- 统计图表和复杂分析。
- mood 输入、真实快照、复盘删除和复杂复盘模板。
- 大模型总结或 AI 助手。
- 大规模 UI 重做或复杂主题系统。

## 7. 关键风险和决策

- 数据库结构变更必须写 migration，不能恢复破坏性删库 fallback。
- `TaskEntity` 当前没有 `completedAt`，`updatedAt` 会被完成、回退、编辑、改期污染；因此当前文案使用“当前已完成”，不承诺精确“今日完成”统计。
- 提醒功能依赖设备侧条件：精确闹钟授权、电池优化策略、通知音量和厂商通知开关。
- 系统 `SpeechRecognizer` 在部分设备上依赖在线语音服务；无网时先提示“当前语音识别需要联网”。
- 发布前必须优先处理数据备份/恢复和隐私政策，避免可用功能变成长期使用风险。

## 8. 推荐下一步

推荐按以下顺序推进：

1. 完成 R1、R2、R4、R5，让 App 具备发布候选的基本外观和构建能力。
2. 收口 R9 的异常/仪器验证，再单独推进 R10 数据层安全债。
3. 完成 R14 隐私政策入口，收口 R12。
4. 完成 R15、R16、R17，补足核心测试和公共 UI 组件整理。

如果只能先做一件事，优先补齐 R9 的真实 Room 仪器测试和提醒重建验收；基本恢复路径已经可用，接下来要证明失败路径不会伤害现有数据。
